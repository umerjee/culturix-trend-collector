fastapi
uvicorn[standard]
SQLAlchemy
psycopg2-binary
httpx
python-dotenv
anthropic
voyageai
numpy
hdbscan
praw
deep-translator
langdetect
apscheduler
# Workstream 2 — new collectors
apify-client
google-api-python-client
# Workstream 3 — Qdrant
qdrant-client
# Workstream 4 — LangGraph pipeline
langgraph
langchain
langchain-community
openai
resend
PyJWT
# Workstream 5 — Media generation (voiceover: free, no API key)
edge-tts
stripe
cryptography
# Faceless-reel media generation ("reel" type in app/media/service.py) —
# script -> voiceover -> N segment images -> ffmpeg render. Voice is
# edge-tts (already listed above, free/no key) with word-level timestamps
# captured directly from its own event stream (confirmed live:
# edge_tts.Communicate's boundary="WordBoundary" param, not the default
# "SentenceBoundary") — no local TTS/transcription model needed. An earlier
# version of this pipeline (Phase 7 clips.py, since removed) ran a
# self-hosted Coqui XTTS v2 + faster-whisper stack instead — CPU-bound,
# 20-60s per script, ~1-2GB of model weights, and a ~1.5-2.5GB PyTorch CPU
# wheel dependency purely to support it. None of that is needed now.
ffmpeg-python
pillow
# CultureToons image uploads (POST .../image routes use FastAPI's UploadFile,
# which needs python-multipart to parse multipart/form-data)
python-multipart
